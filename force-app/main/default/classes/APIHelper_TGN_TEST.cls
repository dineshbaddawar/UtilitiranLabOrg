/* Generated by TestGen on Thu Apr 14 2022 at 6:23 AM (GMT) */
@isTest
private class APIHelper_TGN_TEST{
    private class APIHelper_HCM_TGN_TEST implements HttpCalloutMock{
        protected Integer code;
        protected String status;
        protected String body;
        protected Map<String,String> headers;
        public APIHelper_HCM_TGN_TEST(Integer code,String status,String body,Map<String,String> headers){
            this.code=code;
            this.status=status;
            this.body=body;
            this.headers=headers;
        }
        public HTTPResponse respond(HTTPRequest req){
            HttpResponse res=new HttpResponse();
            res.setStatusCode(this.code);
            res.setStatus(this.status);
            res.setBody(this.body);
            if(this.headers!=null){
                for(String key:this.headers.keySet()){
                    res.setHeader(key,this.headers.get(key));
                }
            }
            return res;
        }
    }
    @isTest
    private static void updateRecords_TGN_TEST(){
        Test.startTest();
        APIHelper_HCM_TGN_TEST httpMock;
        httpMock=new APIHelper_HCM_TGN_TEST(200,'Complete',Json.serialize(null),null);
        Test.setMock(HttpCalloutMock.class,httpMock);
        APIHelper.updateRecords();
        Test.stopTest();
    }
    @isTest
    private static void deleteRecords_TGN_TEST(){
        Test.startTest();
        APIHelper_HCM_TGN_TEST httpMock;
        httpMock=new APIHelper_HCM_TGN_TEST(200,'Complete',Json.serialize(null),null);
        Test.setMock(HttpCalloutMock.class,httpMock);
        APIHelper.deleteRecords('a');
        Test.stopTest();
    }
    @isTest
    private static void APIHelper_TGN_TEST(){
        APIHelper obj = new APIHelper();
    }
}